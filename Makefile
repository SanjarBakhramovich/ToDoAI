# Команда для облегчения git commit и push
# Выполняет git add, commit и push с пользовательским сообщением коммита.
# Этот таргет предназначен для упрощения процесса коммита и отправки изменений в репозиторий.
git:
	@read -p "Введите сообщение коммита: " msg; \
	git add .; \
	git commit -m "$$msg"; \
	git push -u origin main